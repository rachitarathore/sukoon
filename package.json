{
  "name": "sukoon",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered emotional support web application for students and young adults",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
